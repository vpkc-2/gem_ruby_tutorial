# VpkcPalindrome

First gem created with Learn enough Ruby 

Detector of Palindrome
## Installation

Add this line to your application's Gemfile:

```ruby
gem 'vpkc_palindrome'
```

And then execute:

    $ bundle install

Or install it yourself as:

    $ gem install vpkc_palindrome

